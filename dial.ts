import { QDial, QDialSignals, QWidget } from '@nodegui/nodegui';

class MyQDial extends QDial {
  constructor(parent: QWidget) {
    super(parent);

    // Set any initial properties for the QDial here...
  }
}
